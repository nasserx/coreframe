# Styles

Purpose: Holds future shared styling assets and style organization files.

Belongs here: global style modules, design-token organization files, and shared styling resources when the styling system grows.

Must not be placed here: components, feature logic, route files, JavaScript utilities, or assets that belong under `assets`.
