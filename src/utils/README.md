# Utils

Purpose: Contains future small, pure utility functions.

Belongs here: framework-agnostic helpers that are stable, reusable, and not tied to a specific feature.

Must not be placed here: React hooks, components, API clients, mutable state, business workflows, constants, or utilities that belong inside a feature.
