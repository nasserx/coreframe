# Constants

Purpose: Holds future shared constant values.

Belongs here: stable values reused across the application that are not environment-specific configuration.

Must not be placed here: secrets, environment variables, feature-private constants, mutable state, business logic, or values that should come from configuration.
