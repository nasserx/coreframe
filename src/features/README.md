# Features

Purpose: Groups future user-facing product capabilities by domain or workflow.

Belongs here: feature-owned modules that combine UI, state, validation, and behavior for a specific product area when implementation begins.

Must not be placed here: generic shared components, global configuration, cross-feature utilities, route definitions, or low-level service clients.
