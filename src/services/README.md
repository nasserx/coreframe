# Services

Purpose: Holds future application service modules that coordinate external systems or platform-level concerns.

Belongs here: service abstractions for integrations, persistence boundaries, telemetry, or other application-wide capabilities.

Must not be placed here: React components, hooks, route files, feature UI, raw constants, or direct page-specific business logic.
