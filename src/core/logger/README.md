# Logger

Purpose: Documents the future location for application-wide logging infrastructure.

Belongs here: logger interfaces, adapters, and logging configuration when logging is implemented.

Must never be placed here: analytics events, monitoring dashboards, feature logic, UI components, API clients, or ad hoc debugging output.
