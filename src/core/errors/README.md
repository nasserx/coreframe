# Errors

Purpose: Documents the future location for shared error infrastructure.

Belongs here: application-wide error types, normalization boundaries, and error handling primitives when they are implemented.

Must never be placed here: feature-specific error messages, UI feedback components, business logic, API request code, logging transports, or page code.
