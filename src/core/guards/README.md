# Guards

Purpose: Documents the future location for application-wide guard boundaries.

Belongs here: cross-cutting route, permission, environment, or runtime guards when those concerns are implemented.

Must never be placed here: authentication implementation, feature-specific rules, UI components, business workflows, API clients, or page code.
