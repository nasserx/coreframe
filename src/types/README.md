# Types

Purpose: Stores future shared TypeScript types.

Belongs here: cross-cutting interfaces, type aliases, and ambient type declarations used by multiple areas of the app.

Must not be placed here: feature-private types, validation schemas, runtime constants, implementation code, or generated types without a clear ownership policy.
