# API

Purpose: Provides a future location for API boundary code.

Belongs here: request definitions, endpoint modules, transport adapters, and API-specific typing when API integration is introduced.

Must not be placed here: UI components, React hooks, global stores, feature workflows, server route handlers, or unrelated utility functions.
